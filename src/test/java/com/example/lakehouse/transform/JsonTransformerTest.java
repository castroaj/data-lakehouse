package com.example.lakehouse.transform;

import org.junit.jupiter.api.Test;

class JsonTransformerTest {

    // TODO: test valid JSON parse, malformed row routing to dead letter, metadata column appends
    // Uses a local SparkSession in local[1] mode — no cluster required
}
